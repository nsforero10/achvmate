const API_URL = "http://localhost:3000";

async function run() {
  console.log("1. Registering user...");
  // Let's use the local API if we have a register route, or directly seed the DB!
  // Wait, in our system, /api/auth/register is in the Next.js app on port 3002
}
run();
